const divide = (a,b) => {
    console.log(`dividing ${a}`);
    console.log(`dividing ${b}`);
    return a / b;
}
divide(128,8);

const square = (x) => x * x;

const add = (a,b) => {
    console.log(`adding ${a}`);
    console.log(`adding ${b}`);
    return a + b;
}
add(a,b);